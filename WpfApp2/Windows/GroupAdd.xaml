<Window x:Class="WpfApp2.Windows.GroupAdd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2.Windows"
        mc:Ignorable="d"
        Title="GroupAdd" Height="300" Width="400">
    <Grid Background="FloralWhite">
        <StackPanel Margin="20">
            <Label Style="{StaticResource lbl}" Content="Группа"/>
            <TextBox Background="Pink" x:Name ="groupTextBox"  Style="{StaticResource tb}" Text="{Binding group}"/>
            <Button Background="Gray" Style="{StaticResource btn}" Name="SaveButton" Content="Добавить" Click="SaveButton_Click"/>
            <Button Background="Gray" Style="{StaticResource btn}" Name="DeleteButton" Content="Удалить" Click="DeleteButton_Click"/>
            <Button Background="Gray" Style="{StaticResource btn}" Name="ExitButton" Content="Выйти" Click="ExitButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
